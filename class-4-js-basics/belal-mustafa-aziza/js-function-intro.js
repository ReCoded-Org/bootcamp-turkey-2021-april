// Partners: Belal Awad && Mustafa Guldag && Aziza 

// 1) 
function minutesToSeconds (minutes){
      let sec = 60;
      return minutes*sec;

      // The below line is an easier way for it:
      // return minutes*60;
}
    
    
// 2) 
function basketballPoint (point2,point3) {
      let sum = 0;
      sum += point2 * 2;
      sum += point3 * 3;
      return sum;
    
      // The below line is an easier way for it:
      // return ((2*point2) + (3*point3));
}
    
    
// 3) 
function concatenateFirstAndLast (name, surname) {
      let x = name;
      let y = surname;
      return "Hello" + x + " " + y;
      
      // The below line is an easier way for it:
      // return "Hello " + name + " " + surname;
}
    
    