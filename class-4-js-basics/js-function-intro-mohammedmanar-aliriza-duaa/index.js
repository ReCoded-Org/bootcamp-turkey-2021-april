function minutesIntoSeconds(minutes) {
    return minutes * 60;
    }
    
    function pointCounter (two, three) {
      let twoPointers = two * 2;
      let threePointers = three * 3;
      return twoPointers + threePointers;
    }
    
    
    function greeting (firstName, lastName) {
      let greetingMessage = "Hello, " + firstName + " " + lastName + "!"
      return greetingMessage
    }